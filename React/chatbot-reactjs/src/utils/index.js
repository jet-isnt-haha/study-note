import { http } from "./http";
import { setToken,getToken,removeToken } from "./token";
import { pathname } from "./pathname";
export{
    http,
    setToken,
    getToken,
    removeToken,
    pathname
}